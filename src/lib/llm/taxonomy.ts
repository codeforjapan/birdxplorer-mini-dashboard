/**
 * 分類・クラスタリングプロンプトが使う「このテーマ固有の判断基準・具体例」。
 *
 * event.ts に置いた値（名称・日付・キーワード等）は文字列を差し替えるだけで
 * 別テーマに転用できるが、ここに置く値はそうではない。「何が誤情報として問題になるか」
 * というテーマの知識そのものであり、別テーマ（別の災害・選挙等）にフォークする際は
 * このファイルの中身を書き直す必要がある。
 *
 * 別テーマへの転用時にテーマ固有の判断基準を直す作業はこのファイルに閉じる
 * ようにする（llm/relevance.ts, llm/assign.ts, llm/recluster.ts のプロンプト構造・
 * 検証ロジック自体は変えなくてよい）。llm/report.ts のプロンプトはフォーマット指示が
 * 中心でテーマ固有の内容をほぼ含まないため、ここには含めていない。
 */

export const TAXONOMY = {
  // ── Stage 1: 関連性判定（llm/relevance.ts） ─────────────────────
  /** 本文中で「ゆるい検索で無関係なノートが混入する」ことを説明する際に挙げる検索語の例。 */
  searchTermExamples: "「熊本」「地震」「津波」",

  /** 高スコア（残す）にする内容の説明＋具体例。 */
  relevantExamples: `- 誤情報・デマの訂正（被害状況・津波・断水断電・義援金詐欺・なりすまし・過去や他災害の
  映像の誤用・地震雲/人工地震/地震予知などの非科学的言説・外国人犯罪や避難所運営の噂 など）
- 訂正の形でなくても、今回の地震に関する被害報告・犠牲者情報・安否・施設の状況・義援金の案内
- 時系列や事実関係の指摘（例:「爆発が起きたのは投稿時刻より後」といった時系列に基づく訂正）
- 今回の地震・災害対応をめぐる政治的な論評、公的機関・政治家の対応への言及や背景補足
- 別の地震・災害（能登・トルコ等）に触れていても、今回の熊本地震の文脈で流通している
  もの（過去映像の誤紐付け訂正など）`,

  /** 低スコア（除外）にする内容の説明＋具体例。 */
  irrelevantExamples: `- 医療・健康のデマ、アニメやイラストのAI生成判定など、地震と関係のない話題
- 地震と無関係な政治・統計の話題（例: 市長の給与、国会議員の所属政党の統計）
- 鉄道事故など、今回の地震と関係のない事象
- 「熊本」が地名として入っているだけの、地震と無関係な一般的話題`,

  // ── Stage 2: クラスタ割当（llm/assign.ts）・Stage 3 の rename（llm/recluster.ts）共用 ──
  /** クラスタ命名の良い例（複数）。llm/assign.ts で使う。 */
  clusterNamingGoodExamples: "「支援物資デマ」「なりすまし公式アカウント」「津波過大予測の誤情報」",
  /** クラスタ命名の悪い例（複数。トピック名そのものになってしまっている）。llm/assign.ts で使う。 */
  clusterNamingBadExamples: "「地震について」「熊本関連」",
  /** クラスタ命名の良い例（1件のみ）。llm/recluster.ts の rename 説明で使う。 */
  clusterNamingGoodExampleSingle: "支援物資デマ",
  /** クラスタ命名の悪い例（1件のみ）。llm/recluster.ts の rename 説明で使う。 */
  clusterNamingBadExampleSingle: "地震について",

  // ── Stage 3: 再編成（llm/recluster.ts） ──────────────────────────
  /**
   * 「表層的には似ているが統合してはいけない」の具体例。過剰統合を防ぐための最重要の例示で、
   * 実際にあった誤統合事故（recluster.ts の冒頭コメント参照）を踏まえている。
   */
  overMergeCounterExample: `「どちらも災害時のデマである」「どちらも避難所が
   舞台である」「どちらも社会不安を煽る噂である」といった共通点は、噂の中身が一致している
   ことを意味しません。たとえば「避難所で特定の宗教への配慮から特定の食事提供をやめた」という
   噂と「災害時に外国人による犯罪が多発している」という噂は、どちらも「災害・避難所・デマ」
   というカテゴリでは似て見えますが、主張の対象（宗教的配慮 vs 外国人犯罪）がまったく別であり、
   統合してはいけません。`,
} as const;
